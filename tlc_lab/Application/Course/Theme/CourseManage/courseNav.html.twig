{#课程内容#}
{% if menu == 'lesson' %}
    <div class="pull-right">
        <button class="btn btn-info btn-sm mts" id="lesson-create-btn" data-toggle="modal" data-target="#modal" data-backdrop="static" data-keyboard="false" data-url="{% if course.type == 'normal' %}{{ path('course_manage_lesson_create', {id:course.id,center:app.request.get('center')}) }}{% endif %}"><i class="glyphicon glyphicon-plus"></i> {% if course.type == 'normal' %}课程内容{% endif %}</button>
        {#<button class="btn btn-info btn-sm" id="lesson-create-btn" data-toggle="modal" data-target="#modal" data-backdrop="static" data-keyboard="false" data-url="{{ path('course_manage_lesson_create_testpaper', {id:course.id,type:2,center:app.request.get('center')}) }}"><i class="glyphicon glyphicon-plus"></i> 练习</button>#}
        <a class="btn btn-info btn-sm mts"  href="{{ U('Course/CourseTestpaperManage/createCourseLesson',{courseId:course.id,center:app.request.get('center')}) }}"><i class="glyphicon glyphicon-plus"></i> 练习</a>
        <div class="btn-group">
            <button type="button" class="btn btn-sm btn-success dropdown-toggle" data-toggle="dropdown">
                <i class="glyphicon glyphicon-plus"></i> {% if setting('default.chapter_name') %}{{setting('default.chapter_name')|default('章')}}{% else %}章{% endif %}/{% if setting('default.part_name') %}{{setting('default.part_name')|default('节')}}{% else %}节{% endif %} <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
                <li>
                    <a href="#" id="chapter-create-btn" data-toggle="modal" data-target="#modal" data-backdrop="static" data-keyboard="false" data-url="{{ path('course_manage_chapter_create', {id:course.id,center:app.request.get('center')}) }}"><i class="glyphicon glyphicon-plus"></i> 添加 {% if setting('default.chapter_name') %}{{setting('default.chapter_name')|default('章')}}{% else %}章{% endif %}</a>
                </li>
                <li>
                    <a href="#" id="chapter-create-btn" data-toggle="modal" data-target="#modal" data-backdrop="static" data-keyboard="false" data-url="{{ path('course_manage_chapter_create', {id:course.id, type:'unit',center:app.request.get('center')}) }}"><i class="glyphicon glyphicon-plus"></i> 添加 {% if setting('default.part_name') %}{{setting('default.part_name')|default('节')}}{% else %}节{% endif %}</a>
                </li>
            </ul>
         </div>
    </div>
{% endif %}
{#授课班#}
{% if menu == 'class' %}
    {% if two_nav == 'class_list' %}
        <a class="btn btn-info btn-sm pull-right mts mrm" href="{{ path('course_manage_class_create', {courseId:course.id,center:app.request.get('center')}) }}" role="button"><i class="glyphicon glyphicon-plus"></i>添加授课班</a>
    {% endif %}
    {% if two_nav == 'class_timecard' %}
        <a class="btn btn-info btn-sm pull-right mts mrm" href="{{ path('course_manage_class_timecard_csv', {courseId:course.id,classId:classId,center:app.request.get('center')}) }}"><i class="glyphicon glyphicon-export"></i> 导出全部打卡数据</a>
    {% endif %}
    {% if two_nav == 'class_test' %}
        <a href="{{ U('Course/CourseTestpaperManage/createCourseTest',{courseId:course.id,classId:conditions.classId,type:conditions.type,center:app.request.get('center')}) }}" class="btn btn-info btn-sm pull-right mts mrm"><span class="glyphicon glyphicon-plus"></span>创建{{ tyepText }}</a>
    {% endif %}
    {% if two_nav == 'timecard_number_list' %}
        <a class="btn btn-info btn-sm pull-right mts mrm" href="{{ path('course_manage_timecard_number_csv', {courseId:course.id,classId:classId,number:number,center:app.request.get('center')}) }}"><i class="glyphicon glyphicon-export"></i> 导出{% if setting('default.user_name') %}{{setting('default.user_name')|default('学员')}}{% else %}学员{% endif %}</a>
    {% endif %}
{% endif %}
{#{% if menu == 'question' %}
    <ul class="nav nav-tabs c-right-nav pull-right">
        <li role="presentation"><a href="{{ U('Course/CourseTestpaperManage/index',{courseId:course.id,center:app.request.get('center')}) }}">试卷管理</a></li>
        <li role="presentation" class="active"><a >题目管理</a></li>
    </ul>
{% endif %}
{% if menu == 'testpaper' %}
    <ul class="nav nav-tabs c-right-nav pull-right">
        <li role="presentation" class="active"><a >试卷管理</a></li>
        <li role="presentation" ><a href="{{ U('Course/CourseQuestionManage/index',{courseId:course.id,center:app.request.get('center')}) }}">题目管理</a></li>
    </ul>
{% endif %}#}


