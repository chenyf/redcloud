{% extends '@User/User/layout.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% set pageNav = 'group' %}

{% block main %}
      <div class="row">
        <div class="col-md-12"> 
          {% if adminGroups %}
          <div class="page-header">
            <h4> 管理的班级</h4>
          </div>
          {% include '@Wclass/my-class/class-ul.html.twig' with {
          'groups':adminGroups,  }
          %}
          {% endif %}
        </div>
        <div class="col-md-12">
          <div class="page-header">
            <h4> 加入的班级</h4>
          </div>
          {% include '@Wclass/my-class/class-ul.html.twig' with {
          'groups':groups,  }
          %}
          
        </div>
        <div class="col-md-12 pull-left">
          {{ web_macro.paginator(paginator) }}
        </div>
      </div>
{% endblock %}